{
  "name": "todoist-cli",
  "version": "0.1.0",
  "description": "Agent-ready CLI for the Todoist API v1 — tasks, projects, sections, labels, comments, sync, activity log",
  "type": "module",
  "bin": {
    "todoist-cli": "./dist/index.js"
  },
  "scripts": {
    "build": "bun build src/index.ts --outfile dist/index.js --target bun",
    "dev": "bun run src/index.ts"
  },
  "dependencies": {
    "commander": "^13.0.0",
    "picocolors": "^1.1.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.7.0"
  }
}
